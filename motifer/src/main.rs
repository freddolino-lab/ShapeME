use motifer;
use std::io;
use std::fs;
use std::env;
use ndarray::prelude::*;
use ndarray_npy;
use std::collections::HashMap;
use serde_pickle::de;

fn main() {

    //let args: Vec<String> = env::args().collect();
    //let config = motifer::parse_config(&args);

    //let arr: Array4<f64> = ndarray_npy::read_npy(config.npy_fname).unwrap();

    //let mut file = fs::File::open(config.pkl_fname);
    //let mut buf_reader = io::BufReader::new(file);
    //let cfg: HashMap<String, f64> = de::from_reader(buf_reader, de::DeOptions::new())
    //    .unwrap();
    

}
