use motifer;
use std::io;
use std::fs;
use std::env;
use ndarray::prelude::*;
use ndarray_npy;
use std::collections::HashMap;
use serde_pickle::de;

fn main() {

    //let args: Vec<String> = env::args().collect();
    //let config = motifer::parse_config(&args);


}
